# vimconfig

An opinionated Neovim config for personal use.

## Install

**One-liner (curl):**
```bash
curl -fsSL kanishkk.xyz/nvim | bash
```

**Manual:**
```bash
git clone https://github.com/Mr-Sunglasses/vimconfig ~/.config/nvim
cd ~/.config/nvim
bash install.sh
```